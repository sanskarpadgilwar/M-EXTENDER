# Normalize line endings: LF in the repo, platform-default on checkout.
* text=auto

# Windows shell scripts must be CRLF to run reliably in cmd.exe.
*.bat text eol=crlf
*.cmd text eol=crlf
*.ps1 text eol=crlf
